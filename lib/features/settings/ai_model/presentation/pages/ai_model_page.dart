import 'package:flutter/material.dart';

class AiModelPage extends StatelessWidget {
  const AiModelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('AI Model')));
  }
}

