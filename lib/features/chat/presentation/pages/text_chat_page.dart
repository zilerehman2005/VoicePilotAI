import 'package:flutter/material.dart';

class TextChatPage extends StatelessWidget {
  const TextChatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Text Chat')));
  }
}

